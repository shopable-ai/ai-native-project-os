# AI Project OS 项目推进骨架与可读性重构设计

- 状态：已批准书面规格
- 日期：2026-07-11
- 方案：标准版方案 B
- 目标：补齐“治理内核已经存在，但人类总入口、推进主流程和标准产物包不足”的结构缺口

本文件是设计权威和实施计划输入，不是可以脱离仓库状态直接运行的提示词。新对话应使用简短 Goal 引用本文件，并要求先读取 `AGENTS.md`、检查 Git 状态、生成实施计划，再按批准计划修改。

## 1. 问题判断

当前仓库已经具备较强的治理内核：L1/L2/L3 边界、框架等级、项目治理路由、受控对象、状态失效、权限、AI 审核、Run/Evidence/Verdict/Claim 和评分边界均有正式定义。

当前主要缺口不是继续增加治理概念，而是缺少一条面向项目执行者的操作骨架：

1. 没有一张把项目生命周期、追溯链、执行链和证据链放在一起的总图；
2. R0、S0—S7 只有高层退出描述，没有逐阶段机器可检查的输入、产物、门禁、证据和重开协议；
3. “需求种子到完成声明”的修正后推进流程散落在多个权威文件中；
4. 链路包、Spec 包、I/O、Workflow、Skill、L3 项目实例缺少标准模板；
5. 能力树、功能树、任务树的生成顺序和权威落点没有统一入口；
6. L1 关键术语没有统一中文语义表；
7. `policies/` 同时承载决策规则和大量 `*-contract.yaml`，长期边界不清；
8. 当前 README、机器成熟度和多个评分快照存在阅读口径冲突。
9. `docs/workflows/` 混入具体 L2 试点步骤和 L1 自身实施计划，通用 SOP 与项目实例材料没有分离；
10. `L2_ONBOARDING`、`PROJECT_TYPE_AND_GOVERNANCE_ROUTING` 等长英文工程名不能直接告诉中文读者文件解决什么问题；
11. 多份文档先给字段、路由、overlay 等工程词，再解释业务目的，新读者无法判断何时使用；
12. `templates/standard-project/` 只有局部说明和少量文件，既没有完整目标树，也没有可执行的标准产物骨架；
13. L3 被写得像所有项目必需层级，没有说明它只在多实例隔离确有必要时启用；
14. `policies/` 缺少中文导航，新读者无法从“要解决的问题”找到对应规则或契约。

## 2. 决策

采用“人类操作骨架 + 机器门禁契约 + 标准产物模板”的补全方式，不立即建设完整运行平台。

本方案新增或强化六类资产：

1. AI Project OS 人类总览和总图；
2. 完整项目推进工作流；
3. R0、S0—S7 阶段出口门禁；
4. L1 关键术语统一表；
5. 链路包、Spec 包、I/O、Workflow、Skill 和项目实例模板；
6. 契约与政策目录边界及相应检查器。

同时采用三层阅读结构：

1. **人类入口层**：中文短文件名，先说明解决什么、何时使用、输入输出和下一步；
2. **模板与示例层**：给出完整目录树、最小可用文件和正反例；
3. **机器约束层**：使用稳定英文标识保存 Schema、策略、枚举和检查器。

L2 是使用 L1 推进的正常目标软件/业务系统。L3 改为按需启用的项目实例隔离层，不再假定每个 L2 都必须包含 `projects/{id}`。

不建设以下能力：

- 持久工作流引擎；
- 多智能体运行平台；
- 生产发布平台；
- 企业项目组合系统；
- 未经研究批准的第三方运行依赖；
- 与当前业务系统绑定的固定业务链路、关键词或商品数据。

## 3. 四条不能混成一条的主线

### 3.1 项目推进控制流

回答“项目下一步应该做什么”：

```text
需求种子/存量系统
→ 项目类型判断
→ 治理路由
→ 阶段定位
→ 研究与认知
→ 场景和链路
→ 架构决策
→ 工程设计
→ 计划与最小实现
→ 验证发布
→ 运行演进
```

### 3.2 产物追溯依赖链

回答“每个产物为什么存在”：

```text
来源
→ 批准事实/需求
→ 场景/触发
→ 业务链路
→ 业务能力/用户功能
→ 架构决策/技术能力/组件/工程链路
→ I/O 契约与验收判据
→ Spec 控制包
→ Task 依赖图
→ Workflow
→ Skill/Tool
→ Run
→ AI 审核与 Evidence
→ 验收裁决
→ 完成声明
→ 复盘与资产演进
```

### 3.3 状态坐标

回答“对象现在处于什么状态”：生命周期、工作状态、审批状态、实现状态、证据等级和失效状态分别记录，禁止用一个 `completed` 代替。

### 3.4 框架和项目配置

回答“系统建设多大、项目采用哪些控制”：框架等级、基础治理配置和可叠加能力互不推导，也不从生命周期或证据等级推导。

## 4. AI Project OS 总图

```mermaid
flowchart TD
    A["需求种子 / 已有系统 / 问题输入"] --> B{"项目类型判断"}
    B -->|存量项目| R0["R0 存量架构恢复"]
    B -->|全新项目| S0["S0 目标与边界"]
    B -->|研究任务| S1["S1 领域与技术认知"]
    R0 --> S0
    S0 --> S1
    S1 --> S2["S2 场景、触发与业务链路"]
    S2 --> S3["S3 方案比较与架构决策"]
    S3 --> S4["S4 工程链路、I/O 与验收设计"]
    S4 --> S5["S5 Spec、任务图、Workflow、Skill 与 MVP"]
    S5 --> S6["S6 Run、测试、Evidence、裁决与发布"]
    S6 --> S7["S7 运行、复盘、资产升格与演进"]
    S1 -->|关键未知未解决| S1
    S3 -->|方案证据不足| S1
    S6 -->|契约或实现失败| S4
    S6 -->|目标错误| S0
    S7 -->|业务假设被推翻| S1
    S7 -->|架构反证| S3
```

总图只作为人类阅读入口。阶段、对象、状态和门禁的机器枚举继续由各自权威维护，禁止在总图文件建立第二套可编辑真相。

## 5. R0、S0—S7 出口门禁

| 阶段 | 必需产物 | 出口门禁 |
|---|---|---|
| R0 存量恢复 | 当前架构、入口清单、反向需求、孤儿实现、差距矩阵、迁移计划 | 所有活动入口已登记；关键实现有来源或标为孤儿；P0/P1 差距有负责人和处置路径 |
| S0 目标边界 | 目标、非目标、角色、成功指标、需求基线、项目类型、治理路由 | 范围和验收分母锁定；负责人批准；重大歧义为零 |
| S1 认知研究 | 来源登记、术语、事实、假设、未知、研究包和研究裁决 | 关键未知已解决、接受风险或存在安全后备路径；关键结论有来源或实验 |
| S2 场景链路 | 场景目录、触发档案、链路包、正常/异常/恢复链、能力视图 | P0/P1 需求均被场景和链路覆盖；每条链有责任主体、失败出口和恢复路径 |
| S3 架构决策 | 质量属性、候选方案、评分、反方报告、ADR、目标架构 | 关键选择有证据、后果、退出条件和重审条件；致命反方问题为零 |
| S4 工程设计 | 组件、工程链路、I/O、失败契约、权限和验收判据 | 跨组件接缝可验证；失败语义、兼容、权限和回滚完整 |
| S5 计划执行 | Spec 五件套、任务依赖图、Workflow、Skill/Tool、最小纵向实现 | 无孤儿任务；任务追溯到 Spec 和验收；工作包可执行、暂停、恢复和失败返回 |
| S6 验证发布 | 测试、Run、Evidence、AI 审核裁决、验收裁决、发布和回滚决策 | 退出码与语义断言通过；P0/P1 为零；证据新鲜；声明不超过证据等级 |
| S7 运行演进 | 指标、事故、成本、恢复记录、复盘和资产生命周期记录 | 运行责任和恢复链成立；反例进入上游重开；可复用资产完成升格或淘汰 |

每个阶段门禁必须形成机器记录，至少包含：阶段、范围、所需对象、所需证据等级、检查命令、结果、未覆盖项、豁免、批准人、验证人、时间、失效条件和重开目标。

## 6. 标准产物包

### 6.1 链路包

```text
templates/chain-package/
├── chain.yaml
├── scenarios.md
├── triggers.yaml
├── business-flow.md
├── exceptions.md
├── recovery.md
├── responsibility-map.md
├── io-map.yaml
├── traceability.md
├── acceptance.md
└── diagrams/
```

链路包属于 L2 业务系统。L1 只保存契约和模板，不保存具体业务链路。

### 6.2 Spec 控制包

```text
templates/spec-package/
├── spec.md
├── plan.md
├── tasks.md
├── acceptance.md
└── traceability.md
```

`tasks.md` 是单个 Spec 的任务权威；跨 Spec 任务总树是生成视图，不建立第二套可编辑任务真相。

### 6.3 I/O 契约模板

至少覆盖 producer、consumer、请求/事件/结果 Schema、成功/失败 envelope、错误分类、重试、幂等、超时、顺序、部分结果、数据分类、兼容、契约测试和迁移。

### 6.4 Workflow 模板

至少覆盖触发、Task 引用、步骤图、执行节点、I/O、权限、预算、超时、重试、checkpoint、取消、补偿、失败路由、终态、Evidence 出口和声明上限。

### 6.5 Skill 模板

至少覆盖消费者、局部职责、输入输出、模型/代码边界、权限、失败返回、评测、版本和兼容范围。Skill 禁止隐藏跨阶段 Workflow。

### 6.6 可选 L3 项目实例模板

至少覆盖项目配置、治理路由锁定、项目事实、数据目录、Run、Evidence、Verdict、Claim、交付物和项目隔离 namespace。

所有模板 README 必须显示复制后的完整目录树，并逐项标记“必需、条件启用、运行时生成”。读者必须能够在不打开内部模板文件的情况下理解最终会得到什么目录。

## 7. 能力树、功能树和任务树

唯一推导顺序：

```text
业务链路 → 业务能力树 → 用户功能视图 → 架构和 Spec
Spec + 验收判据 → Task 依赖图 → Workflow → Skill/Tool
```

推荐 L2 落点：

```text
domain/capability-map/       # 批准业务能力
design/function-map/         # 用户功能视图
chains/{chain_id}/           # 链路包
specs/{spec_id}/tasks.md     # 单个 Spec 的任务权威
views/task-tree.md           # 跨 Spec 生成视图
```

能力树、功能树和任务树均不得保存到 `.prompts/`。原始灵感只能进入来源或候选区，经批准后才进入正式对象。

## 8. L2 是主要目标系统，L3 只在需要实例隔离时启用

L1 AI Project OS 可以用于推进任何具体的软件、服务、内部平台、自动化系统或 AI 原生业务系统，这些正常目标统一称为 L2 业务系统。L2 不属于任何单一具体业务仓库。

L3 只在 L2 内部确实存在多个需要独立治理的实例时启用：

| 模式 | 适用情况 | 数据和运行落点 |
|---|---|---|
| 无实例层 | 方法库、研究项目、无独立运行数据的软件包 | 只保留 L2 事实、设计和验证资产 |
| 单一系统 | 一个软件系统就是一个运行与验收范围 | 项目事实、Run、Evidence 直接按 L2 标准目录保存，不额外套 `projects/{id}` |
| 多实例系统 | 多客户、多品牌、多独立知识库或多个需要隔离治理的项目 | 使用 `projects/{project_id}/` 建立 L3 namespace |

商品或 SKU 不自动等于 L3。如果多个商品共享同一套配置、工作流、权限和运行证据，它们只是 `catalog` 中的数据实体；只有当某个商品项目拥有独立配置、数据权限、Run、Evidence、Verdict 和交付物时，才升级为 L3 项目实例。

现有通用 L1 文档中的具体业务名称和下层私有路径必须移除。具体接入步骤和真实证据保存在对应 L2 仓库；L1 只保留通用接入 SOP、匿名化 fixture 和可重算的消费者兼容结果。

### 8.1 通用 L2 业务系统推进 SOP

```text
识别目标系统
→ 判断为新建型、存量恢复型或研究型
→ 锁定兼容的 L1 版本
→ 建立来源区和批准业务真相区
→ 存量系统先执行 R0
→ 从 S0 开始建立范围和需求基线
→ 按 S1—S7 产出链路、架构、契约、Spec、任务、运行和证据
→ 判断是否需要 L3 实例隔离
→ 执行检查器和验收裁决
→ 保存 L2 自身 Evidence，不把具体项目事实反写到 L1
```

“新建型”对应机器枚举 `greenfield`，“存量恢复型”对应 `brownfield`，“研究型”对应 `research_only`。面向人的 SOP 先使用中文，再在括号或机器示例中提供英文枚举。

## 9. 人类可读性和文件命名

### 9.1 命名规则

- 顶层目录继续使用稳定英文：`docs/`、`contracts/`、`policies/`、`templates/`、`tests/`；
- 面向人的 Markdown 使用短中文名，例如 `总览.md`、`项目推进.md`、`阶段门禁.md`、`业务系统接入.md`、`术语表.md`；
- 机器读取的 YAML、Schema、代码和稳定 ID 使用短英文 kebab-case；
- 历史 ADR、评分快照和已经对外锁定的机器路径不为美观随意改名；
- 发生重命名时使用 Git move，并一次性更新所有链接、机器权威路径、测试和锁文件，禁止保留两份权威文件。

### 9.2 每份人类文档的固定开头

每份人类可读文档必须先回答：

1. 这份文件解决什么问题；
2. 什么情况下需要阅读；
3. 需要什么输入；
4. 会产生什么输出；
5. 完成后下一步去哪里。

正文第一次出现 `greenfield`、`brownfield`、route、overlay、proof 等英文术语时，必须先给中文含义。机器字段细节下沉到契约文件，不能让普通读者从 YAML 字段反推业务目的。

### 9.3 目录导航

`contracts/`、`policies/`、`templates/`、`workflows/` 和 `skills/` 均必须提供中文 README，至少包含“解决的问题 → 使用时机 → 输入 → 输出 → 对应文件 → 示例 → 下一步”的索引表。README 只做导航，不复制字段枚举。

### 9.4 当前可读性诊断

当前人工可读性暂评为 `42/100`，这是面向新项目负责人的启发式诊断，不是 AI Project OS 总体评分：

| 可读性维度 | 当前得分 |
|---|---:|
| 30 秒内知道文件解决什么 | 9/20 |
| 知道什么时候使用 | 5/15 |
| 输入输出清楚 | 7/15 |
| 有完整示例或目录树 | 6/15 |
| 知道下一步去哪里 | 6/15 |
| 通用规则与具体试点分离 | 3/10 |
| 人类说明与机器契约分层 | 6/10 |

此前“中文可读性 92”主要衡量中文说明是否存在，没有检验新读者能否完成任务，因此不能继续作为真实使用体验分。方案实施后的可读性目标不低于 95，并由陌生读者任务测试验证。

## 10. 目标目录结构

```text
ai-project-os/
├── README.zh-CN.md
├── project-os.yaml
├── docs/
│   ├── architecture/
│   │   ├── 总览.md
│   │   ├── 核心架构.md
│   │   ├── 框架等级.md
│   │   └── 分层契约.md
│   ├── workflows/
│   │   ├── 项目推进.md
│   │   ├── 项目生命周期.md
│   │   ├── 阶段门禁.md
│   │   ├── 项目分类与治理.md
│   │   ├── 架构师工作.md
│   │   └── 业务系统接入.md
│   ├── governance/
│   │   ├── 术语表.md
│   │   ├── 受控对象.md
│   │   ├── 状态与失效.md
│   │   ├── 运行证据与验收.md
│   │   └── 门禁与评分.md
│   └── research/
│       └── 研究与技术选型.md
├── contracts/
│   ├── README.md
│   ├── governance/
│   ├── artifacts/
│   ├── execution/
│   └── io/
├── policies/
│   └── README.md
├── templates/
│   ├── README.md
│   ├── system-project/
│   ├── project-instance/
│   ├── chain-package/
│   ├── spec-package/
│   ├── io-contract/
│   ├── workflow/
│   ├── skill/
│   └── research-package/
├── workflows/
│   └── README.md
├── skills/
│   └── README.md
├── linters/
├── adapters/
├── fixtures/
├── tests/
├── decisions/
├── research/
└── reviews/
```

目标目录必须完整定义，但物理目录只在首个正式资产出现时创建。`apps/`、`libs/`、`tools/` 仅在形成真实 CLI、服务或可导入内核时创建，不为未来可能性建立空目录。

`docs/workflows/` 只保存通用项目推进和接入 SOP。L1 自身的建设计划保存到 `plans/` 或内部计划区；具体 L2 试点步骤和业务路径不得出现在通用工作流中。

## 11. `contracts/` 与 `policies/` 的边界

- `contracts/`：保存对象结构、I/O、manifest、版本、失败语义和兼容要求；
- `policies/`：保存路由、风险判断、授权决策、门禁和声明封顶规则。

现有 `policies/*-contract.yaml` 应在实施阶段通过 Git move 迁入 `contracts/governance/`，同时更新 `project-os.yaml`、文档引用、测试和检查器。迁移必须使用一份 ADR 或迁移记录，不能复制后保留两个权威文件。

## 12. 关键术语统一范围

新增 L1 术语权威，至少统一：来源、业务真相、研究、框架等级、基础治理配置、叠加能力、生命周期、工作状态、实现状态、证据等级、业务链路、工程链路、Workflow、Skill、Tool、能力树、功能树、任务图、Spec、Run、Evidence、Verdict 和 Claim。

稳定英文机器标识保留；中文定义必须唯一，其他文档引用术语权威，不能自行给同一词增加第二种含义。

## 13. 评分

### 13.1 当前分数不能写成一个数字

当前存在三个不同口径：

- 历史 P0 设计快照为 84，仅代表当时范围和硬封顶；
- 当前总体评分入口仍有 `not_evaluated`，因此不能把 84 当成当前有效总分；
- 人工规则治理与 AI 审核子方案的目标设计分为 96，但它明确禁止解释为当前得分。

因此，当前 AI Project OS 的有效总体专家评分是 `not_evaluated`。静态契约和检查器已有局部证据，但还不能合并为通用 95+ 声明。

### 13.2 方案 B 的目标设计分

按 `weighted-geometric-v1` 公式，本方案目标维度为：

| 维度 | 目标分 |
|---|---:|
| 系统边界与分层 | 97 |
| 生命周期与推进闭环 | 97 |
| 来源、事实、研究与决策 | 95 |
| 产物职责与追溯 | 97 |
| Workflow、执行和恢复 | 95 |
| AI 原生上下文、模型和工具治理 | 95 |
| 安全、权限与项目隔离 | 95 |
| 测试、证据和完成声明 | 96 |
| 演进、兼容和资产生命周期 | 95 |
| 中文可读性与使用体验 | 98 |

加权几何目标分为 `95.93`，无处罚时可表述为“目标设计 95+”。它是批准方案的设计目标，不是当前证据分、实现分或运行证明分。

## 14. 达到可证明 95+ 的紧急路径

仅补文档不能获得可证明 95+。最短闭环必须同时解除四个硬封顶：

1. **解除 84 封顶**：建立一条端到端 fixture 纵向链，覆盖需求、链路包、Spec、Task、Workflow、Skill/Tool、Run、Evidence、Verdict 和 Claim，并完成暂停/恢复或失败重开实验；
2. **解除 89 封顶**：提供双项目 namespace、权限、缓存、证据和秘密隔离的正反例测试；
3. **解除 94 封顶**：由独立于设计和实现主体的反方评审者重新评分，保存逐维原始分、处罚项、命令和 Evidence；
4. **允许声明通用 95+**：至少两个异构真实 L2 项目完成只读或本地接入验证，不能使用同一业务复制品充数。

紧急路径可以把生产运行排除在评分 scope 外，声明范围限定为“standard 框架设计、契约测试、fixture 运行和异构 L2 本地/只读验证”。如果声明包含生产就绪，还必须增加经批准生产 Run、外部回执、恢复演练和责任链，不能用上述 95+ 替代生产证明。

## 15. 实施顺序

1. 清除 L1 文档中的具体 L2 项目名称和路径，把试点操作移回 L2 仓库；
2. 修正 README、`project-os.yaml.maturity` 和评分入口冲突；
3. 建立中文短文件名、三层阅读结构和目录 README；
4. 新增总览、项目推进、阶段门禁、业务系统接入和术语权威；
5. 分离 contracts 与 policies；
6. 建立链路包、Spec、I/O、Workflow、Skill 和可选 L3 实例模板；
7. 扩展检查器，验证阶段门禁、模板包完整性、术语漂移、业务绑定和图覆盖；
8. 建立端到端正反例 fixture；
9. 完成第一个 L2 存量系统接入并把具体证据保存在 L2；
10. 完成第二个异构 L2 系统接入；
11. 独立反方复核并生成不可变评分快照。

## 16. 验收条件

- 存在唯一的人类总览入口，能够在一页内理解四条主线；
- 所有人类入口使用短中文名，并在开头说明问题、时机、输入、输出和下一步；
- L1 通用文档不包含具体业务系统名称、路径、业务术语或试点操作；
- L2 被定义为正常目标软件系统，L3 仅在多实例隔离有证据时启用；
- R0、S0—S7 每阶段均有输入、必需产物、出口门禁、Evidence 和重开目标；
- 链路包、Spec、I/O、Workflow、Skill 和 L3 项目实例模板均可被检查器扫描；
- 能力树、功能树、任务图的生成顺序和唯一权威落点明确；
- `contracts/` 与 `policies/` 不再混放；
- L1 术语只有一个中文定义位置；
- contracts、policies、templates、workflows 和 skills 均有中文问题导航；
- 陌生读者能够在不打开 YAML 的情况下完成“判断项目类型—定位阶段—找到下一步模板”任务；
- README 与机器成熟度、评分入口和工作树事实一致；
- 当前分、目标设计分、静态实现分、fixture 证明、本地证明和生产证明分别报告；
- 任何 95+ 声明均绑定不可变快照、可重放命令、独立审查和明确 scope。
